.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
*.db
*.db-wal
*.db-shm
.DS_Store
# `just add` writes each bench's inventory and output here.
.demo-logs/
# The README's run-it instructions create this in the working directory.
bench.json
# The event log a failing chaos run dumps for evidence.
chaos-seed-*-events.jsonl
